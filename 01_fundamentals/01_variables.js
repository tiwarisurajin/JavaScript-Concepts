
// What is variable ? 

// A variable is a name binding to a value. If the value is an object, the binding provides access to that object.

//  Declaration 
let a;   // Declaration of binding or variable
 a = 10;  // Assigning a value to variable

let place ;
place = 'Gorkha'

// Initialization 

//means Declaration + Assignment together 

let name = "Sarthak";

Note : "We cannot declare  const variable without Initializing it "

// Difference between var, let and const

let v = 10 ;   // let creates an variable that can be reassigned
const  x = 10 ; // const variables can't be reassigned they are initialized once and remain fixedc

var less_used = "right" ; // this variable is old and its valeus can also be reassigned but because of scope problem this is not used widely in code.