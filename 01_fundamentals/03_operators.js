// What is operator?
 "Operator is a symbol or keyword that tells Programming languages to perform an operation on one or more operands and produce a result"

 "e.g"

 a = 10 
 b = 20 
 c = a + b ;
 // here + is an operator

 

/*    ARITHMATIC OPERATORS */

 /*  +   for addition
     -   for subtraction
     *   for multiplication
     /   for division
     %   (Remainder) for remainder devision  to get remainer 
     //   Exponantiation for doing power multiplication like square      cubes 
    */

     "Coercions"

     "There are two types of coercions implicit and explicit "

     c = "5"-2
     console.log(c); /* answer will be int(3) because Javascript did coercion itself and its known as */ "Implicit Coercion"

    // Exception 
     "Here + is  special "
     
     a = "5"  + 2   ;

     console.log(a); 
     /* Note : Unless all the valeus are numbers or float , + sign will perform concatination rather  than addition. *
     
     // Assignment Operator 
     x = 

     // Comparision Operator


     > greater than 
     < less than 
     >= greator than equal to
     <= less than equal to
     == loose equality
     != loose inequality

     === strict Equality
     !== strict Inequality


*/
console.log(null == null);
console.log(null === null);
console.log(null != null);
console.log(null !== null);

console.log(""
);

console.log(null == undefined);
console.log(null ===undefined);
console.log(null != undefined);
console.log(null !== undefined);




console.log(NaN == NaN);
console.log(NaN===NaN);
console.log(NaN != NaN);
console.log(NaN !== NaN);


